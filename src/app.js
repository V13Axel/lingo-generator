// Alpine component factory. Real content added in Phase 5.
// Must be a standard function (not arrow) so Alpine can bind `this`
// to the reactive component proxy when evaluating the returned object.
export function lingoApp() {
  return {
    init() {
      // no-op until Phase 5
    },
  };
}
